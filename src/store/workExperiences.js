const initState = [];

const workExperienceReducer = (state = initState, action) => {
  switch (action.type) {
    case "ADD":
      return;
    case "UPDATE":
      return;
    case "REMOVE":
      return;
    default:
      return state;
  }
};

export default workExperienceReducer;
