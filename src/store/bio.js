const initState = {
  id: "",
  name: "",
  email: "",
  shortBio: "",
};

const bioReducer = (state = initState, action) => {
  if (action.type === "ADD") {
  }
  if (action.type === "UPDATE") {
  }
  return state;
};

export default bioReducer;
